#!/bin/bash

set -e

# start styleguide in background
docker-compose up -d ca-styleguide

BACKSTOPJS_VERSION=`npm view backstopjs version` docker-compose run visual-tests reference --config=backstop-config-ci.js
