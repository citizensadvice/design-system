#!/bin/bash

set -e

# start styleguide in background
docker-compose up -d ca-styleguide

# run visual regression tests
BACKSTOPJS_VERSION=`npm view backstopjs version` docker-compose run visual-tests
