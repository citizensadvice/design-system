/**
 * @deprecated
 */
const initTables = () => {
  // eslint-disable-next-line no-console
  console.info(
    'deprecation warning: initTables is no longer required and will be removed in future versions.'
  );
};

export default initTables;
