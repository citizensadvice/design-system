<%= form_with model: @person, url: request.path do |f| %>
  <% options = { required: params[:required], hint: params[:hint], page_heading: params[:page_heading] } %>
  <%= f.send(:cads_date_field, :date_of_birth, **options) %>

  <%= f.submit %>
<% end %>
