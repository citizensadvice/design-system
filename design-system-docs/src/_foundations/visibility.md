ArgumentsTable---
title: Visibility
---

There are a number of ways to show/hide text visually and in the context of a screen reader. Use the inspect tools to see what the gap below is.

<%= render(Shared::ComponentExample.new("visibility_default")) %>

<%= render(Shared::ComponentExample.new("visibility_hide_breakpoint_only")) %>
<%= render(Shared::ComponentExample.new("visibility_hide_breakpoint_up")) %>
<%= render(Shared::ComponentExample.new("visibility_show_breakpoint_up")) %>
<%= render(Shared::ComponentExample.new("visibility_show_breakpoint_only")) %>

