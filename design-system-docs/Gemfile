# frozen_string_literal: true

source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

gem "bridgetown", "~> 1.3"
gem "bridgetown-seo-tag"
gem "bridgetown-view-component"

gem "htmlbeautifier"
gem "nokogiri"
gem "puma"

gem "citizens_advice_components", path: "../engine"

# ViewComponent is bundled by the design system but include
# it explicitly as we use it for example components in the
# demo app, and so we can control the version if necessary.
gem "view_component", "~> 3.23"

group :development, :test do
  gem "citizens-advice-style",
      github: "citizensadvice/citizens-advice-style-ruby",
      tag: "v12.0.0"
end
