# i18n-tasks finds and manages missing and unused translations: https://github.com/glebm/i18n-tasks
base_locale: en
data:
  read:
    - locales/%{locale}.yml
  write:
    - locales/%{locale}.yml
search:
  paths:
    - haml/
  only: ['*.html.haml']
