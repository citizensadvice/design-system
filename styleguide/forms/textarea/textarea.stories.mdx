import { Meta, Story, Preview, Source } from '@storybook/addon-docs/blocks';
import { translate } from '../../../scripts/story-helpers';

<Meta title="Forms/Textarea" />

# Textarea

## Variant: Basic input

import templateTextarea from './_textarea.html.haml';

<Preview>
  <Story
    name="Basic"
    parameters={{ docs: { source: { code: templateTextarea.raw } } }}
  >
    {(_, options) => translate(templateTextarea, options)}
  </Story>
</Preview>

## Variant: With hint

import templateTextareaWithHint from './_textarea-with-hint.html.haml';

<Preview>
  <Story
    name="With hint"
    parameters={{ docs: { source: { code: templateTextareaWithHint.raw } } }}
  >
    {(_, options) => translate(templateTextareaWithHint, options)}
  </Story>
</Preview>

## Variant: With optional field marker

import templateTextareaOptional from './_textarea-optional.html.haml';

<Preview>
  <Story
    name="Optional field"
    parameters={{ docs: { source: { code: templateTextareaOptional.raw } } }}
  >
    {(_, options) => translate(templateTextareaOptional, options)}
  </Story>
</Preview>

import templateTextareaWithError from './_textarea-with-error.html.haml';

## Variant: With error message

<Preview>
  <Story
    name="With error"
    parameters={{ docs: { source: { code: templateTextareaWithError.raw } } }}
  >
    {(_, options) => translate(templateTextareaWithError, options)}
  </Story>
</Preview>

## Haml template options

The Haml partial takes an `input` hash with the following properties:

| properties      | Description                                                 |
| --------------- | ----------------------------------------------------------- |
| `name`          | Required, field name                                        |
| `label`         | Required, label text                                        |
| `hint`          | Optional, hint text                                         |
| `optional`      | Optional, boolean flag to mark the field as optional        |
| `error_message` | Optional, error message for the field                       |
| `has_error`     | Optional, boolean flag to mark the field as having an error |
| `rows`          | Optional, textarea rows. Defaults to 8                      |
