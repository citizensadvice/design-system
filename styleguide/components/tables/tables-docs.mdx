import { Story, Preview } from '@storybook/addon-docs/blocks';

# Tables

<Preview>
  <Story id="components-tables--table" />
</Preview>

## Haml template options

The Haml partial takes a `table_data` hash with the following properties:

| Property                   | Description                                             |
| -------------------------- | ------------------------------------------------------- |
| `caption`                | Optional, table caption              |
| `header[]`                    | Required, an array of header titles |
| `rows[]`              | Required, array of tuples containing row values                   |
