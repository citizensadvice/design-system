import { Meta, Story, Preview, Source } from '@storybook/addon-docs/blocks';
import { translate } from '../../../scripts/story-helpers';

<Meta
  title="Components/Callouts"
  decorators={[(Story) => `<div style="max-width: 800px">${Story()}</div>`]}
/>

# Callouts

There are 4 main callout styles in our Design system:

## Info callout

This callout style should be used on important, non-mandatory advice for users that don’t have any serious or legal implications. There might be other implications.

import templateStandard from './_standard.html.haml';

<Preview>
  <Story
    name="Standard callout"
    parameters={{ docs: { source: { code: templateStandard.raw } } }}
  >
    {(_, options) => translate(templateStandard, options)}
  </Story>
</Preview>

## Example callout

This style is when an example needs to be displayed. Also, use it for quotes.

import templateExample from './_example.html.haml';

<Preview>
  <Story
    name="Example callout"
    parameters={{ docs: { source: { code: templateExample.raw } } }}
  >
    {(_, options) => translate(templateExample, options)}
  </Story>
</Preview>

## Important callout

The important callout should be used for any important snippet of text that has serious and/or legal implications if the user does not follow the advice

import templateImportant from './_important.html.haml';

<Preview>
  <Story
    name="Important callout"
    parameters={{ docs: { source: { code: templateImportant.raw } } }}
  >
    {(_, options) => translate(templateImportant, options)}
  </Story>
</Preview>

## Adviser callout

Content that is for advisers. This will only show on the logged-in public site and AdviserNet.

import templateAdviser from './_adviser.html.haml';

<Preview>
  <Story
    name="Adviser callout"
    parameters={{ docs: { source: { code: templateAdviser.raw } } }}
  >
    {(_, options) => translate(templateAdviser, options)}
  </Story>
</Preview>

## When to use

Callouts can be used to highlight information within content, for example:

- Important information 
- Adviser-only information
- Examples
- Quotes

Before using a callout, make sure you really need it. Fewer callouts have more of an impact on a piece of content. Too many callouts can confuse and distract users from other important content on a page. 

## Usage

By default, all callouts come with a title that explains the subject of the information. This will give the users an idea of what the information is about. There are some exceptions where a callout doesn’t have a title. Ensure a title is labelled in the code so the screen reader can identify and announce.

### Markup

The title should be used as `h3` no matter where they are on the page. If a callout is the first thing on the page under a `h1`, still put it in the backend as a `h3` and the CMS will make sure it displays correctly.


## Haml template options

The Haml partial takes a `notice` hash with the following properties:

| Property         | Description                                                       |
| ---------------- | ----------------------------------------------------------------- |
| `notice['type']` | Optional type, one of: `standard`, `example`, `import`, `adviser` |
| `notice['body']` | HTML string                                                       |

For example:

<Source dark="true" language="haml" code={templateImportant.raw} />
