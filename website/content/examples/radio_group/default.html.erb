---
title: Default
---

<% @model = Forms::Radios.valid_example %>
<%= form_with model: @model, url: "/" do |form| %>
  <%= form.cads_collection_radio_buttons(
    :radios,
    @model.options,
    :id,
    :name
  ) %>
<% end %>
